{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "dotenv": "^16.3.1",
    "hardhat": "^2.18.3"
  },
  "scripts": {
    "deploy:attratest": "npx hardhat run --networ attratest scripts/deploy",
    "verify:attratest": "npx hardhat verify --network attratest --constructor-args deploy/args.js --contract contracts/Faucet.sol:Faucet"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
